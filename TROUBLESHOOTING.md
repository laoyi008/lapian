# 故障排查指南

## 🔍 视频分析无法正常工作

### 问题现象
点击"开始分析"后，分析无法正常进行或出现错误。

---

## 📋 排查步骤

### 第1步：打开开发者工具

1. **按 F12** 键打开浏览器开发者工具
2. **点击 Console** 标签页
3. **清空控制台** （点击🚫图标）
4. **重新上传视频** 并点击"开始分析"
5. **观察控制台输出**

---

### 第2步：检查控制台日志

#### ✅ 正常情况应该看到：

```
✓ 正在提取视频关键帧...
✓ 开始分析 8 个镜头
✓ 正在分析第 1/8 个镜头，时间戳: 0秒
✓ 提交第 1 个镜头的图像分析请求...
✓ 图像数据长度: XXXXX 字符
✓ 请求参数: { hasImage: true, hasUrl: false, ... }
✓ 响应状态: 200 OK
✓ 图像分析响应: { status: 0, data: { ... } }
✓ 第 1 个镜头提交成功，任务ID: task_xxx
✓ 开始轮询任务结果，任务ID: task_xxx
✓ 轮询尝试 1/30
✓ 任务状态码: 1 (处理中)
✓ 任务处理中，等待 2000ms 后重试...
✓ 轮询尝试 2/30
✓ 任务状态码: 0 (完成)
✓ 任务完成，返回结果
✓ 第 1 个镜头分析完成
✓ 分析结果: ...
✓ 进度更新: 36.2%
```

#### ❌ 如果看到错误：

---

### 第3步：根据错误类型排查

## 错误类型1：HTTP错误

### 错误信息示例：
```
❌ HTTP错误 400: Bad Request
❌ HTTP错误 401: Unauthorized
❌ HTTP错误 500: Internal Server Error
```

### 可能原因：
1. **网络连接问题**
2. **API密钥配置错误**
3. **请求参数格式错误**
4. **服务器临时故障**

### 解决方案：

#### 方案1：检查网络连接
```bash
# 在控制台执行
fetch('https://www.baidu.com').then(r => console.log('网络正常'))
```

#### 方案2：检查环境变量
```bash
# 查看 .env 文件
VITE_APP_ID=your_app_id  # 确保已配置
```

#### 方案3：查看详细错误
```
查看控制台中的 "错误响应内容" 日志
```

---

## 错误类型2：API返回错误999

### 错误信息示例：
```
❌ API返回错误999: 权限不足
❌ API返回错误999: 配额已用完
```

### 可能原因：
1. **API权限未开通**
2. **API配额已用完**
3. **API密钥过期**

### 解决方案：

#### 检查API配置
1. 登录百度智能云控制台
2. 检查API是否已开通
3. 查看API调用配额
4. 确认API密钥有效

---

## 错误类型3：图像数据问题

### 错误信息示例：
```
❌ 图像数据长度: 0 字符
❌ 响应中缺少task_id
```

### 可能原因：
1. **视频格式不支持**
2. **视频文件损坏**
3. **关键帧提取失败**

### 解决方案：

#### 方案1：检查视频格式
- ✅ 支持：MP4、MOV、AVI
- ❌ 不支持：FLV、RMVB等

#### 方案2：尝试其他视频
- 使用10-30秒的短视频测试
- 确保视频清晰、未损坏
- 建议使用720p或1080p

#### 方案3：查看关键帧提取日志
```
查看控制台中 "正在提取视频关键帧..." 后的日志
```

---

## 错误类型4：任务超时

### 错误信息示例：
```
❌ 图像分析超时，请稍后重试
❌ 轮询尝试 30/30
```

### 可能原因：
1. **网络速度慢**
2. **服务器处理慢**
3. **图像过大**

### 解决方案：

#### 方案1：等待更长时间
- 当前超时时间：60秒
- 可以刷新页面重试

#### 方案2：使用更短的视频
- 建议视频时长：10-30秒
- 减少关键帧数量

#### 方案3：检查网络速度
```bash
# 测试网络延迟
ping api-integrations.appmiaoda.com
```

---

## 错误类型5：所有帧分析均失败

### 错误信息示例：
```
❌ 所有帧分析均失败
❌ 分析第 1 帧失败: ...
❌ 分析第 2 帧失败: ...
```

### 可能原因：
1. **API持续返回错误**
2. **网络不稳定**
3. **视频内容问题**

### 解决方案：

#### 方案1：查看每帧的具体错误
```
查看控制台中每个 "分析第 X 帧失败" 的详细错误信息
```

#### 方案2：重新上传视频
1. 刷新页面
2. 重新上传视频
3. 再次尝试分析

#### 方案3：更换视频
- 尝试使用其他视频
- 确保视频内容清晰
- 避免纯黑或纯白画面

---

## 🛠️ 高级调试

### 查看完整请求信息

在控制台执行：
```javascript
// 查看请求参数
console.log('请求参数:', {
  hasImage: true,
  imageLength: 12345,
  questionLength: 200
});

// 查看响应数据
console.log('响应数据:', responseData);
```

### 手动测试API

```javascript
// 测试图像分析API
const testAPI = async () => {
  const response = await fetch(
    'https://api-integrations.appmiaoda.com/app-7z9dk2hyx5hd/api-prXa6prqL6dE/rest/2.0/image-classify/v1/image-understanding/request',
    {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'X-App-Id': 'your_app_id'
      },
      body: JSON.stringify({
        image: 'base64_image_data',
        question: '描述这张图片'
      })
    }
  );
  
  const data = await response.json();
  console.log('API响应:', data);
};

testAPI();
```

---

## 📊 常见问题汇总

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| 进度卡在0% | 视频加载失败 | 检查视频格式和大小 |
| 进度卡在10% | 关键帧提取失败 | 更换视频或刷新页面 |
| 进度卡在30% | API调用失败 | 检查网络和API配置 |
| 显示HTTP错误 | 网络或服务器问题 | 检查网络连接 |
| 显示999错误 | API权限问题 | 检查API配置和配额 |
| 任务超时 | 处理时间过长 | 使用更短的视频 |
| 所有帧失败 | 持续性错误 | 查看详细错误日志 |

---

## 💡 最佳实践

### 视频要求
- ✅ 格式：MP4、MOV
- ✅ 时长：10-60秒
- ✅ 大小：<50MB
- ✅ 质量：720p或1080p
- ✅ 内容：清晰、完整

### 使用建议
1. **首次使用**：先用10秒短视频测试
2. **网络环境**：确保网络稳定
3. **浏览器**：使用Chrome或Edge最新版
4. **错误处理**：遇到错误先查看控制台
5. **重试策略**：失败后刷新页面重试

---

## 🆘 仍然无法解决？

### 收集以下信息：

1. **控制台完整日志**
   - 按F12 → Console → 右键 → Save as...

2. **视频信息**
   - 格式：____
   - 时长：____秒
   - 大小：____MB
   - 分辨率：____

3. **错误信息**
   - 完整的错误消息
   - 错误发生的步骤
   - 错误截图

4. **环境信息**
   - 浏览器：____
   - 操作系统：____
   - 网络环境：____

### 联系技术支持

提供上述信息，以便快速定位问题。

---

## 📚 相关文档

- [HOW_TO_DEBUG.md](HOW_TO_DEBUG.md) - 快速调试指南
- [API_DOCUMENTATION.md](API_DOCUMENTATION.md) - API使用文档
- [USER_GUIDE.md](USER_GUIDE.md) - 用户使用指南

---

**更新日期：** 2025-12-03  
**版本：** v2.1  
**状态：** ✅ 持续更新
